{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Style</th>
                <td>{{ categorie.style }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categorie.description }}</td>
            </tr>
        </tbody>
    </table>

    <p><a href="{{ path('app_categorie_index') }}">Retourner à la liste</a></p>

    <p><a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}">Modifier</a></p>
<p></p>
    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
